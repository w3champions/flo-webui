overwrite: true
schema: "http://localhost:3558"
documents: "graphql/**/*.graphql"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
  ./generated/graphql.schema.json:
    plugins:
      - "introspection"
